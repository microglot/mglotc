name: test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - "**"

jobs:
  tests:
    name: tests
    steps:
      - uses: actions/checkout@v4
      - name: make test
        run: make test
